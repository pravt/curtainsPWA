// const PrismicDOM = require('prismic-dom');

import * as React from "react";
import styled from "styled-components/macro";
import PropTypes from "prop-types";

const Page = (props) => {
  return <div>No page template</div>;
};

export default Page;
